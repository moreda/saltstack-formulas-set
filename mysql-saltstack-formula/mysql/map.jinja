{% set mysql = salt['grains.filter_by']({
    'default': {
        'config': '/etc/mysql/my.cnf',
        'pkg_server': 'mysql-server',
        'service_server': 'mysql',
        'pkg_client': 'mysql-client',
        'pkg_python': 'python-mysqldb',
    },
}, merge=salt['pillar.get']('mysql:lookup')) %}
